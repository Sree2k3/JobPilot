# JobPilot - Dependencies
# Install all at once: pip install -r requirements.txt

# ── Phase 1: Intake Pipeline ──
pandas>=2.0.0
python-dotenv>=1.0.0

# ── Phase 2: Resume Parsing ──
pdfplumber>=0.10.0
PyPDF2>=3.0.0
python-docx>=1.0.0
gdown>=5.0.0

# ── Phase 2 (optional): LLM Enhancement ──
openai>=1.0.0

# ── Phase 3: Job Scraping ──
selenium>=4.20.0
playwright>=1.40.0        # optional if using Playwright instead of Selenium

# Parsing helpers
beautifulsoup4>=4.12.0
lxml>=5.0.0

# ── Testing ──
pytest>=8.0.0

# ── Utilities ──
requests>=2.30.0
httpx>=0.25.0
